/*
const sortReducer = (state, action) => {
  switch (action.type) {
    case 'ALPHA ASC':
      return
    case 'ALPHA DESC':
      return
    case 'YEAR ASC':
      return
    case 'YEAR DESC':
      return
    default:
      return state
  }
}

export default sortReducer

*/
